apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service-deployment
  namespace: onlineeventbooking
  labels:
    app: Notifications
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Notifications
  template:
    metadata:
      labels:
        app: Notifications
    spec:
      containers:
      - name: notifications
        image: localhost:32000/notifications:latest
        ports:
        - containerPort: 5003
        env:
          - name: PORT
            value: "5003"
          # FIX: Pass RabbitMQ URL via config (was hardcoded localhost in code)
          - name: RABBITMQ_URL
            valueFrom:
              configMapKeyRef:
                name: eventsphere-config
                key: RABBITMQ_URL
        resources:
          requests:
            cpu: "250m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: onlineeventbooking
spec:
  selector:
    app: Notifications
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
  type: ClusterIP
